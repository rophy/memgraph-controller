{{ if .Values.secrets.enabled }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ .Release.Name }}
stringData:
  MEMGRAPH_USER: {{ .Values.secrets.username }}
  MEMGRAPH_PASSWORD: {{ .Values.secrets.password }}
{{ end }}
