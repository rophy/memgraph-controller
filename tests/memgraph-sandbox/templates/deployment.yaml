apiVersion: apps/v1
kind: Deployment
metadata:
  name: neo4j-client
  labels:
    app: neo4j-client
spec:
  replicas: 1
  selector:
    matchLabels:
      app: neo4j-client
  template:
    metadata:
      labels:
        app: neo4j-client
    spec:
      containers:
      - name: neo4j-client
        image: neo4j-client
        imagePullPolicy: IfNotPresent
        env:
        - name: NEO4J_URI
          value: "bolt://memgraph-sandbox:7687"
        - name: WRITE_INTERVAL
          value: "1000"
        - name: NEO4J_USERNAME
          value: "memgraph"
        - name: NEO4J_PASSWORD
          value: ""
        resources:
          requests:
            memory: "128Mi"
            cpu: "100m"
          limits:
            memory: "256Mi"
            cpu: "200m"
        livenessProbe:
          exec:
            command:
            - node
            - -e
            - "process.exit(0)"
          initialDelaySeconds: 10
          periodSeconds: 30
        readinessProbe:
          exec:
            command:
            - node
            - -e
            - "process.exit(0)"
          initialDelaySeconds: 5
          periodSeconds: 10